#include <stdlib.h>
#include <stdio.h>
#include "list.h"
/*
int
main(int argc, char *argv[])
{
  Coor c = {10,11};
  c.tag[0]='h';
  c.tag[1]='o';
  c.tag[2]='l';
  c.tag[3]='a';
  printf("%s:(%d, %d)\n",c.tag,c.x,c.y);
  exit(EXIT_SUCCESS);
}
*/
int
main(int argc, char *argv[])
{
  List * l;
  l=newList();
  exit(EXIT_SUCCESS);
}
